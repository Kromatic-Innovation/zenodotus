# Unrelated-negation fixture

This fixture proves negation must attach to the verb occurrence, not the
whole sentence: the `flimflam` binary runs the packaging step every time,
though this paragraph is not exhaustive about every edge case.
